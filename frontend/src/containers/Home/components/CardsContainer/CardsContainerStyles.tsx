import { css } from '@emotion/react';

export const container = css`
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    row-gap: 5rem;
    column-gap: 3rem;
    padding: 2rem 5rem;
    margin-top: 10rem;
`;
